import { Jost, Gabarito, Climate_Crisis } from 'next/font/google';

export const jost = Jost({
    subsets: ['latin'],
});

export const gabarito = Gabarito({
    subsets: ['latin'],
});

export const climateCrisis = Climate_Crisis({
    subsets: ['latin'],
});